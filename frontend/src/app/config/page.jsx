"use client";

/** Config editor — mirrors config.yaml fields. Step 6. */
export default function ConfigPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-semibold">Config</h1>
      <p className="mt-2 text-neutral-500">Step 6 — edit config.yaml via API.</p>
    </main>
  );
}
