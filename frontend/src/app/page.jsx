"use client";

/** Dashboard — episode grid + New Episode modal. Step 4. */
export default function DashboardPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-semibold">Podcast Dashboard</h1>
      <p className="mt-2 text-neutral-500">Step 4 — implement episode list + NewEpisodeForm.</p>
    </main>
  );
}
